{
    "benchmarks": [{
        "name": "ls",
        "executable": "/bin/ls",
        "commands": [{
            "args": "."
        }]
    }, {
        "name": "cat",
        "executable": "/usr/bin/cat",
        "commands": [{
            "args": "/proc/self/maps"
        }]
    }]
}
