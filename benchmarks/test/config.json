{
    "benchmarks": [{
        "name": "ls",
        "commands": [{
            "command": "/bin/ls ."
        }]
    }, {
        "name": "cat",
        "commands": [{
            "command": "/usr/bin/cat /proc/self/maps"
        }]
    }]
}
